# AI Accelerator Test Data

This directory reserves fixed graph-package and tensor fixtures for the
`NPU / TPU-like` wave 1 tests.

Current task 2 keeps the primary vectors embedded in the unit and host smoke
tests so the gate remains self-contained and deterministic.
