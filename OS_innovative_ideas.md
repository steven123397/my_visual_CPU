# OS 内核实现 —— 创新方向与扩展建议

> 本文档服务于《操作系统课程设计》OS 内核实现方案（难度系数 100%）。
> 文档定位为**开放式创新清单**：记录本项目在算法、功能、架构三个维度的可选创新方向，供各 agent / 开发者补充、评审和选择性实施。
>
> **使用规则**：
> - 每个创新点按「级别 / 创新点 / 说明 / 工作量 / 与现有资产的关联」组织。
> - 新增条目请按章节追加，不要删除已有条目。
> - 每个 agent 的署名区独立，方便追溯贡献来源。
> - 实施某个创新点后，请在对应条目下追加「实施状态」子节，避免重复提案。

---

## 目录

- [一、项目已有资产盘点（创新基础）](#一项目已有资产盘点创新基础)
- [二、算法层面创新](#二算法层面创新)
  - [2.1 调度算法](#21-调度算法)
  - [2.2 内存管理算法](#22-内存管理算法)
  - [2.3 文件系统算法](#23-文件系统算法)
- [三、功能层面创新](#三功能层面创新)
  - [3.1 AI Accelerator 系统调用（NPU 方向）](#31-ai-accelerator-系统调用npu-方向)
  - [3.2 JIT/DBT 与系统调用结合](#32-jitdbt-与系统调用结合)
  - [3.3 Pipeline OoO 感知](#33-pipeline-ooo-感知)
  - [3.4 浏览器前端 Lab 可视化](#34-浏览器前端-lab-可视化)
  - [3.5 `/proc` 虚拟文件系统](#35-proc-虚拟文件系统)
  - [3.6 Shell 管道链与脚本支持](#36-shell-管道链与脚本支持)
- [四、架构层面创新](#四架构层面创新)
  - [4.1 微内核 / 混合内核探索](#41-微内核--混合内核探索)
  - [4.2 安全与隔离](#42-安全与隔离)
  - [4.3 容器 / 虚拟化方向](#43-容器--虚拟化方向)
- [五、推荐组合方案（按投入产出排序）](#五推荐组合方案按投入产出排序)
- [六、Agent 署名与贡献区](#六agent-署名与贡献区)

---

## 一、项目已有资产盘点（创新基础）

在提出创新方向前，先明确本项目已有的独特资产。这些资产是其他普通 OS 课程项目不可能具备的，也是创新设计的根基。

| 资产 | 当前状态 | 可为创新提供的支撑 |
|---|---|---|
| **RISC-V 系统模拟器（myCPU）** | 已可运行，支持 M/S/U 特权级、CSR、Sv39 MMU、TLB | 老师允许使用自己的模拟器运行 OS，不需要绑定 QEMU |
| **AI Accelerator（NPU-like MMIO 设备）** | 完整设备骨架：DMA engine、scratchpad、graph scheduler、per-op profile、guest driver、中断闭环 | 可向 OS 暴露系统调用，让用户态程序直接提交 AI 推理任务 |
| **JIT/DBT 原型** | 完整 dry-run 链：`DbtBlockPlan` → `translator` → `IR v0` → `lowering` → `host emitter` → `executable cache` | 可向 OS 暴露性能 profile，让调度器感知翻译状态 |
| **Pipeline OoO 执行** | `rename` / `ROB` / `LSQ` / `speculation` 全部就绪，已有 debug snapshot | 可向 OS 暴露流水线阻塞状态，让调度器做性能感知决策 |
| **浏览器前端 Lab 工作台** | `/console` 支持 terminal、寄存器、CSR、设备面板、AI accelerator 计数器 | 可用于实时可视化进程状态、内存分布、NPU 推理过程 |
| **Linux Console 路线** | 已通过 `virtio-blk` 跑通外部 Alpine/Debian rootfs | 可向真实发行版方向扩展，展示 OS 兼容性 |
| **完整的测试与构建体系** | `make test` / `make test-pipeline` / guest smoke / host smoke | 创新实现后可用现有体系验证，不另起炉灶 |

> **关键认知**：本项目的创新空间不在于"把课程要求做一遍"，而在于"把课程六大模块与上述独有资产做有机连接"。

---

## 二、算法层面创新

### 2.1 调度算法

课程要求：实现 FCFS 和 RR 两种调度算法，支持多个并发进程，时间片可配置（默认 10ms），进程切换开销 < 1ms。

#### 2.1.1 多级反馈队列（MLFQ）

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 在 RR 基础上扩展为 3 级队列：高优先级短任务（4ms）、中优先级（8ms）、低优先级 FCFS。进程在时间片内未完成任务则自动降级，等待过久则自动升级。 |
| **工作量** | ⭐ 约 200 行代码，可复用现有 RR 的时间片逻辑 |
| **与现有资产关联** | 可直接复用 `user_task_t` 和 `trap_user_runtime` 的上下文切换路径 |
| **教学价值** | 展示 "短任务优先 + 防饥饿" 的经典权衡，是很多真实调度器的简化骨架 |
| **实施状态** | 待实施 |

#### 2.1.2 优先级继承（Priority Inheritance）

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 在已有信号量/互斥锁基础上，解决优先级反转问题。当高优先级进程阻塞在低优先级进程持有的锁上时，临时提升低优先级进程的优先级至阻塞者的优先级，直到锁释放。 |
| **工作量** | ⭐ 约 150 行代码，需为信号量增加 `owner` 和 `priority_ceiling` 字段 |
| **与现有资产关联** | 复用 `user_task` 的优先级字段和现有的信号量原语 |
| **教学价值** | 经典实时系统问题，Mars Pathfinder 就是因为这个问题出故障 |
| **实施状态** | 待实施 |

#### 2.1.3 CFS 简化版

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 实现一个教学级完全公平调度器：用排序链表（红黑树太复杂可降级）维护每个进程的 `vruntime`，每次选择 `vruntime` 最小的进程运行。新进程 `vruntime` 初始化为当前队列最小值，保证公平性。 |
| **工作量** | ⭐⭐⭐ 约 500 行代码，需重新设计调度器数据结构 |
| **与现有资产关联** | 替代现有调度器核心，与 `user_task` 的激活/切换路径对接 |
| **教学价值** | 这是 Linux 真实调度器的骨架，理解 CFS 就是理解现代 OS 调度 |
| **实施状态** | 待实施 |

#### 2.1.4 性能感知调度（Pipeline-Aware Scheduling）⭐ 本项目独有

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 利用 Pipeline 后端已有的 `ROB`、`LSQ` 状态做调度决策。当某进程的 `LSQ::oldest_load_status()` 报告 blocked load 过多、或 `ROB` 深度不足导致流水线停滞时，主动切换进程，让物理流水线"喘息"。调度器定期读取 Pipeline debug snapshot 中的 `rob_occupancy`、`lsq_blocked_loads` 等指标，将其纳入调度权重。 |
| **工作量** | ⭐⭐⭐ 需打通 `Machine` → `PipelineBackend` → `debug_snapshot` → 调度器的只读观测链，约 300 行 |
| **与现有资产关联** | **直接依赖** `pipeline` 后端的 `debug_snapshot()` 和 `LoadStoreQueue::oldest_load_status()` |
| **教学价值** | **课程内几乎不可能有人做的方向**。展示 "OS 调度器与 CPU 微架构的协同优化"，是学术界的前沿方向（如 Google 的 PerCpu Scheduling） |
| **实施状态** | 待实施 |

---

### 2.2 内存管理算法

课程要求：物理页框分配器、虚拟内存（页表创建/映射/解除映射）、kmalloc/kfree、用户态内存布局、支持至少 4MB 物理内存、页大小 4KB、支持 Demand Paging、无泄漏。

#### 2.2.1 SLAB 分配器

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 在 `kmalloc/kfree` 基础上增加对象缓存：为常用结构体（`struct file`、`struct inode`、`struct proc`）预分配固定大小的对象池，减少页级分配开销。每个缓存维护一个 `slab` 列表，每个 slab 包含若干同色（相同大小）对象。 |
| **工作量** | ⭐ 约 200 行代码，可复用现有 `pmm_alloc_page` |
| **与现有资产关联** | 复用 `pmm` 的位图分配器作为底层页供应 |
| **教学价值** | 这是 Linux 的 `kmem_cache` 简化版，展示 "对象级分配 vs 页级分配" 的性能差异 |
| **实施状态** | 待实施 |

#### 2.2.2 Clock 页面置换算法

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 在 Demand Paging 基础上增加页回收策略：为每个页表项维护访问位（A-bit，利用 RISC-V PTE 的 `A` 位），用单指针循环扫描页表，实现近似 LRU。当物理内存耗尽时触发页面置换，避免 panic。 |
| **工作量** | ⭐ 约 150 行代码，复用 Sv39 页表的 PTE 访问位 |
| **与现有资产关联** | 复用 `vm_address_space_t` 的页表遍历逻辑 |
| **教学价值** | 经典近似 LRU 算法，理解 "访问位 + 时钟指针" 的核心思想 |
| **实施状态** | 待实施 |

#### 2.2.3 伙伴系统（Buddy System）

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 把现有的位图分配器升级为真正的 2 的幂次分配/合并伙伴系统。每个阶数（order）维护一个空闲块链表，分配时向上取整到最近的 2 的幂次，释放时检查伙伴是否空闲并合并。消除外部碎片。 |
| **工作量** | ⭐⭐⭐ 约 400 行代码，需重新设计物理内存元数据结构 |
| **与现有资产关联** | 替换 `pmm.c` 的核心分配逻辑，接口保持不变 |
| **教学价值** | Linux 物理内存分配的真实算法，理解 "分裂/合并/伙伴" 的递归结构 |
| **实施状态** | 待实施 |

#### 2.2.4 WSClock（工作集时钟）

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 结合工作集理论和 Clock 算法：为每个进程维护工作集窗口（最近 N 次时钟中断内访问过的页面集合），优先置换不属于任何进程工作集的页面，同时用 Clock 指针降低全页表扫描开销。 |
| **工作量** | ⭐⭐⭐ 约 500 行代码，需维护进程级工作集记录 |
| **与现有资产关联** | 复用 `vm_address_space_t` 和 `vm_process_t` 的进程级结构 |
| **教学价值** | 展示 "工作集模型 + Clock 近似" 的融合，Denning 的经典理论 |
| **实施状态** | 待实施 |

#### 2.2.5 写时复制 Fork（COW Fork）⭐ 强烈推荐

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 在 `fork()` 时不立即复制父进程的全部物理页框，而是：父子进程共享所有用户页框，将页表项同时标记为只读；任一方首次写入时触发 store page fault → 分配新物理页 → 复制原始内容 → 更新双方页表。利用 RISC-V PTE 的 `W` 位和 Sv39 的 page fault 机制实现。配合现有的 `vm_object_t` 引用计数，当共享页只剩最后一个引用时自动恢复可写权限，避免不必要的 COW 触发。 |
| **工作量** | ⭐ 约 200 行代码，复用 `vm_fault.c` 的 store page fault 路径和 `vm_object_t` 的 `attachment_count` |
| **与现有资产关联** | **直接复用** `vm_object_t` 的引用计数、`vm_fault.c` 的 `vm_handle_page_fault()` store fault 分支、`vm.c` 的 `map_page_internal()` 和 PTE 操作 |
| **教学价值** | 这是现代 OS fork 的标配优化——展示 "延迟复制 + 引用计数 + fault-driven" 的经典组合。答辩时可以用 fork 一个大进程（64KB+）对比 COW 和 naive fork 的耗时差异，数据说服力极强 |
| **实施状态** | 待实施 |

---

### 2.3 文件系统算法

课程要求：简化文件系统（RAMFS 或简化 EXT2）、文件/目录 CRUD、文件描述符表、路径解析（绝对/相对）、标准 IO 重定向、至少 128 个文件、单文件最大 64KB、目录层级至少 3 层、支持 seek、提供 mkfs 工具。

#### 2.3.1 B 树目录索引

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 在简单线性目录表之上增加 B 树索引，让 `ls` 和路径解析从 O(n) 降到 O(log n)。每个目录维护一个 B 树，key 为文件名 hash，value 为 inode 号。教学级可用简化的 2-3 树或 B+ 树。 |
| **工作量** | ⭐ 约 250 行代码 |
| **与现有资产关联** | 复用 `storage_read_block` 作为底层块读写 |
| **教学价值** | 数据库/OS 交叉知识点，展示 "索引结构如何改变访问复杂度" |
| **实施状态** | 待实施 |

#### 2.3.2 写时复制快照（COW Snapshot）

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 利用已有的匿名对象（anon backing）机制，为文件系统增加 `snapshot` 命令。创建快照时不复制数据，只在写入时触发页级 COW：当进程尝试修改被快照引用的数据页时，内核复制该页并重新映射。 |
| **工作量** | ⭐ 约 200 行代码，复用 `vm_object_t` 的引用计数和 `anon backing` |
| **与现有资产关联** | **直接复用** `vm_object_t` 的 `attachment_count` 和 `anon backing` 页槽机制 |
| **教学价值** | 展示 "引用计数 + 写时复制" 的经典设计，ZFS/Btrfs 的核心思想 |
| **实施状态** | 待实施 |

#### 2.3.3 日志结构文件系统（LFS）简化版

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 所有写操作追加到日志末尾，定期清理（cleaning）回收无效块。 inode 映射表（Imap）维护 inode 号到日志位置的映射，确保读取时无需扫描整个日志。 |
| **工作量** | ⭐⭐⭐ 约 600 行代码，需重新设计块布局 |
| **与现有资产关联** | 复用 `storage` 块设备接口 |
| **教学价值** | 展示 "顺序写优势" 和 "清理开销" 的权衡，Flash 存储的底层逻辑 |
| **实施状态** | 待实施 |

#### 2.3.4 自适应预读块缓存

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 在块设备驱动和文件系统之间插入一层带自适应预读的块缓存：内核维护一个固定大小的 LRU 缓存（如 64 个块），每次 `read_block()` 先查缓存。当检测到连续 2 次顺序读时自动将预读窗口从 1 块扩大到 2 块，连续顺序读则继续扩大到 4 块、8 块；一旦检测到随机读（LBA 跳跃 >1）则收缩窗口回到 1。缓存命中率通过 `/proc/blockstat` 暴露。 |
| **工作量** | ⭐ 约 200 行代码，在现有 `storage_read_block()` 调用链上增加缓存层 |
| **与现有资产关联** | 复用 `storage` 块设备接口和 `pmm_alloc_page` 做缓存页分配 |
| **教学价值** | 展示 "预读 + LRU + 自适应窗口" 三层设计，理解 Linux `readahead` 的核心思想。答辩时展示同一文件在有无预读时的吞吐量对比曲线 |
| **实施状态** | 待实施 |

---

## 三、功能层面创新

### 3.1 AI Accelerator 系统调用（NPU 方向）⭐ 本项目独有

这是**其他课程项目绝对不可能有的方向**。仓库已有完整的 `MMIO NPU` 设备（`KMVAI`）、guest driver、graph package 提交/完成/中断闭环。

#### 3.1.1 `sys_ai_submit` 系统调用

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 用户态程序通过系统调用向 NPU 提交推理任务（graph package）。内核负责：DMA buffer 映射（用户态 buffer → scratchpad 地址空间）、doorbell 写入、等待 completion IRQ、将结果复制回用户态。 |
| **工作量** | ⭐ 约 300 行代码，复用 `ai_accel.c` 的 guest driver |
| **与现有资产关联** | **直接复用** `guest/kernel/ai_accel.c` 的 `doorbell` / `completion` / `counter` ABI |
| **教学价值** | 展示 "用户态 → 内核态 → 设备 MMIO → 中断 → 用户态" 的完整设备驱动闭环 |
| **实施状态** | 待实施 |

#### 3.1.2 NPU 中断驱动调度

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | NPU completion IRQ 触发进程从**阻塞态 → 就绪态**。进程调用 `sys_ai_submit` 后进入阻塞，NPU 完成后由 PLIC external interrupt 唤醒。这是展示 "设备驱动 + 进程状态转换" 的经典闭环。 |
| **工作量** | ⭐ 约 150 行代码，复用现有的 timer/external interrupt 唤醒路径 |
| **与现有资产关联** | 复用 `trap_dispatch.c` 的 external interrupt handler 和 `user_task` 的状态机 |
| **教学价值** | 展示 "I/O 阻塞 → 中断唤醒 → 调度器重新调度" 的真实流程 |
| **实施状态** | 待实施 |

#### 3.1.3 AI Shell 命令

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 在 shell 中增加 `ai-run <model>` 命令，用户直接调用 NPU 推理并查看结果。命令解析 graph package 路径，通过 `sys_ai_submit` 提交，等待结果后打印推理输出和性能摘要（`device_cycles / dma_cycles / compute_cycles`）。 |
| **工作量** | ⭐⭐⭐ 约 400 行代码，需实现用户态 graph package 加载器和 AI libc wrapper |
| **与现有资产关联** | 复用 `workloads/ai_proto/pack_graph.py` 生成的 graph package 格式 |
| **教学价值** | 答辩演示效果极好，"我们在自己的 OS 里运行了 NPU 推理" |
| **实施状态** | 待实施 |

#### 3.1.4 AI 任务调度器（NPU 资源调度）

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 当多个用户进程同时提交 NPU 任务时，内核维护一个 NPU 任务队列，按优先级或 FIFO 调度 NPU 资源。展示 "多进程竞争有限硬件资源" 的调度问题。 |
| **工作量** | ⭐⭐⭐ 约 400 行代码，需为 NPU 设备增加软件队列 |
| **与现有资产关联** | 复用 `AiAccelerator` 的 MMIO 控制面和 `ai_submission_queue` |
| **教学价值** | 向 "真实 NPU driver" 迈出的关键一步，展示硬件资源的多路复用 |
| **实施状态** | 待实施 |

---

### 3.2 JIT/DBT 与系统调用结合 ⭐ 本项目独有

仓库已有完整的 JIT/DBT dry-run 基础设施：`DbtBlockPlan` → `translator` → `IR v0` → `lowering` → `host emitter` → `executable cache`。

#### 3.2.1 `sys_jit_profile` 系统调用

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 暴露 JIT dry-run 的统计信息（hit / miss / emit / fallback / invalidate 计数），用户程序可查询自身的热点代码翻译状态。 |
| **工作量** | ⭐ 约 100 行代码，复用 `dbt_runtime_harness` 的 summary 结构 |
| **与现有资产关联** | **直接复用** `dbt_runtime_harness` 的 `hit / miss / emit / exec / fallback / invalidate` 计数 |
| **教学价值** | 展示 "OS 向用户态暴露硬件/翻译器状态" 的设计理念 |
| **实施状态** | 待实施 |

#### 3.2.2 JIT 辅助的用户态 `printf`

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 把频繁调用的 `write` 系统调用路径在 JIT 翻译层做部分内联优化：JIT 识别到 `write(fd=1)` 模式时，生成直接写 UART MMIO 的宿主代码（在安全边界内），减少 trap 开销。 |
| **工作量** | ⭐ 约 200 行代码，需扩展 translator 的 pattern 识别 |
| **与现有资产关联** | 复用 `dbt_translator` 的 reject taxonomy，新增 `write-to-uart` helper plan |
| **教学价值** | 展示 "JIT 如何优化高频系统调用路径"，类似 vDSO 的思想 |
| **实施状态** | 待实施 |

#### 3.2.3 JIT 沙箱隔离

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 利用 DBT translation 做用户态程序的安全沙箱：在 JIT 翻译阶段插入边界检查（memory access range check），被翻译的代码无法越界访问用户态地址空间。非法访问触发 translator reject，回退到 reference fallback。 |
| **工作量** | ⭐⭐⭐ 约 400 行代码，需扩展 `dbt_ir_lowering` 的 memory op 路径 |
| **与现有资产关联** | **直接扩展** `dbt_translator` 的 memory helper 和 `dbt_ir_lowering` 的 load/store op |
| **教学价值** | 展示 "动态翻译 + 安全检查" 的软硬件协同安全设计 |
| **实施状态** | 待实施 |

#### 3.2.4 自适应 JIT 调度

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 调度器根据 JIT 翻译状态做决策：已被翻译的进程分配更长时间片（因为执行更快），未翻译进程分配更短时间片（鼓励其积累翻译热点）。 |
| **工作量** | ⭐⭐⭐ 约 250 行代码，需打通 JIT metadata → 调度器的观测链 |
| **与现有资产关联** | 复用 `dbt_block_cache` 的 hit/miss 统计和 `dbt_jit_engine` 的 dispatch 结果 |
| **教学价值** | 展示 "调度器与代码优化的协同"，学术界前沿方向 |
| **实施状态** | 待实施 |

---

### 3.3 Pipeline OoO 感知 ⭐ 本项目独有

仓库已有 `rename` / `ROB` / `LSQ` / `speculation` 的完整实现。

#### 3.3.1 `sys_perf` 系统调用

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 暴露 Pipeline 只读计数器：`rob_occupancy`、`lsq_blocked_loads`、`speculation_mispredicts`。shell 的 `perf` 命令可以打印这些信息。 |
| **工作量** | ⭐ 约 100 行代码，复用 `PipelineBackend::debug_snapshot()` |
| **与现有资产关联** | **直接复用** `pipeline_backend` 的 `debug_snapshot` 和 `LoadStoreQueue` 的计数器 |
| **教学价值** | 展示 "OS 如何观测和利用微架构状态" |
| **实施状态** | 待实施 |

#### 3.3.2 调度器感知流水线阻塞

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 当 `LSQ::oldest_load_status()` 报告有 blocked load 时，调度器优先选择另一个就绪进程切换，避免 CPU 空转等待内存。 |
| **工作量** | ⭐ 约 150 行代码，在调度决策点增加 Pipeline 状态查询 |
| **与现有资产关联** | **直接复用** `LoadStoreQueue::oldest_load_status()` 和 `PipelineBackend::debug_snapshot()` |
| **教学价值** | 展示 "OS 调度器根据 CPU 微架构状态动态决策" |
| **实施状态** | 待实施 |

#### 3.3.3 SMT-like 逻辑多核

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 利用已有的 `Physical Register File` 和 `Rename Map`，在硬件层面模拟两个逻辑线程：共享 fetch/decode，独立的 rename/ROB/LSQ。调度器可以将两个进程同时调度到同一物理核心上。 |
| **工作量** | ⭐⭐⭐⭐ 约 800 行代码，需大幅扩展 Pipeline 核心 |
| **与现有资产关联** | **深度改造** `pipeline_backend`、`rename_map`、`physical_register_file` |
| **教学价值** | 向 Intel Hyper-Threading 的教学级实现迈进 |
| **实施状态** | 待实施（建议作为远期 stretch） |

---

### 3.4 浏览器前端 Lab 可视化

前端 Lab 工作台是答辩展示的利器。以下可视化方案需要前后端协同。

#### 3.4.1 进程状态机实时动画

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 前端用 canvas 展示进程状态切换（就绪 → 运行 → 阻塞 → 僵尸）的时序图，配合颜色区分不同进程，支持时间轴拖拽回放。 |
| **工作量** | ⭐ 前端约 300 行 JS，后端约 100 行 C（暴露进程状态快照） |
| **与现有资产关联** | 复用 `/console` 的现有前端框架和 debug CLI 的 JSON 协议 |
| **教学价值** | 直观展示 "进程生命周期"，答辩时让评委"一眼看懂" |
| **实施状态** | 待实施 |

#### 3.4.2 内存分布热力图

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 展示物理内存页框的分配状态（空闲 / 已用 / 内核 / 用户），MMU 页表 walk 的可视化路径（点击虚拟地址 → 展示三级页表遍历 → 最终物理页）。 |
| **工作量** | ⭐ 前端约 250 行 JS，后端约 150 行 C（暴露 pmm/vm 状态） |
| **与现有资产关联** | 复用 `pmm` 的 `used_pages` / `free_pages` 和 `vm_debug_walk` |
| **教学价值** | 直观展示 "虚拟地址 → 物理地址" 的映射过程 |
| **实施状态** | 待实施 |

#### 3.4.3 文件系统树可视化

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 前端展示 inode 树结构、文件描述符表、打开文件列表，支持展开/折叠目录节点。 |
| **工作量** | ⭐ 前端约 200 行 JS，后端约 100 行 C |
| **与现有资产关联** | 复用现有 debug CLI 协议 |
| **教学价值** | 直观展示文件系统的树形结构 |
| **实施状态** | 待实施 |

#### 3.4.4 调度过程交互式回放

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 记录一次调度周期的完整 trace（每个 tick 的进程状态、寄存器快照、CPU 占用），前端支持拖拽时间轴、单步回放每个进程的上下文切换瞬间。 |
| **工作量** | ⭐⭐⭐ 前端约 500 行 JS，后端约 300 行 C（trace buffer + snapshot） |
| **与现有资产关联** | 复用 `debug snapshot` 协议和 `trap_user_runtime` 的上下文结构 |
| **教学价值** | 答辩时可以"慢动作回放"一次进程切换，展示 trap 帧、页表切换、寄存器保存/恢复 |
| **实施状态** | 待实施 |

#### 3.4.5 NPU 推理过程可视化

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 在 AI Shell 命令执行时，前端实时展示 DMA load → compute → DMA store 的 pipeline 阶段，以及 per-op 的 `compute_cycles / stall_cycles`。可以用进度条或甘特图展示每个阶段的时间占比。 |
| **工作量** | ⭐⭐⭐ 前端约 400 行 JS，后端约 200 行 C（暴露 NPU profile 实时流） |
| **与现有资产关联** | **直接复用** `AiAcceleratorProfileSummary` 的 per-op summary 和 timing 字段 |
| **教学价值** | **答辩演示的王炸效果**：用户在前端看到 NPU 推理的每一步，配合 shell 输出结果 |
| **实施状态** | 待实施 |

#### 3.4.6 全栈可观测教学 OS 面板 ⭐ 强烈推荐

| 属性 | 内容 |
|---|---|
| **级别** | 轻量到深度可分阶段 |
| **说明** | 把课程要求的六大模块统一纳入一个可观测面板：进程表、就绪 / 阻塞 / 僵尸队列、调度器当前算法与时间片、系统调用 trace、trap / interrupt trace、页表 walk、缺页与 COW fault、PMM 页框热力图、文件描述符表、inode / 目录树、block cache 命中率等。面板只消费内核和模拟器暴露的只读 snapshot，不参与 OS 语义决策。 |
| **工作量** | ⭐ 起步约 300 行前端 + 150 行 debug snapshot；完整版本约 800 行 |
| **与现有资产关联** | 复用 `/console` Lab、`debug_snapshot`、`debug_cli`、`vm_debug_walk`、`pmm` 统计、未来 `procfs` 与 syscall trace |
| **教学价值** | 将普通课程项目的「能跑」升级为「能解释」。答辩时可以同时展示用户命令、内核状态变化和模拟器观测证据，形成区别于 QEMU 黑盒运行的核心创新点 |
| **实施状态** | 待实施 |

---

## 四、架构层面创新

### 4.1 微内核 / 混合内核探索

#### 4.1.1 文件系统服务进程化

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 把文件系统核心逻辑（inode 管理、路径解析）拆成一个独立的用户态服务进程。其他进程通过 `sys_send` / `sys_recv` 消息调用 FS 服务。展示 "宏内核 → 微内核" 的演进。 |
| **工作量** | ⭐ 约 400 行代码，复用 `user_task` 和 trap 机制 |
| **与现有资产关联** | 复用 `user_task` 的用户态执行和 `trap_user_runtime` 的上下文切换 |
| **教学价值** | 展示微内核的核心思想：服务进程化 + 消息传递 |
| **实施状态** | 待实施 |

#### 4.1.2 设备驱动用户态化

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 把块设备驱动拆成用户态进程，通过共享内存 + 中断完成 I/O。内核只保留最小特权代码（中断路由、MMIO 映射设置）。 |
| **工作量** | ⭐ 约 350 行代码 |
| **与现有资产关联** | 复用 `storage` 的 MMIO 接口和 `trap` 的中断处理 |
| **教学价值** | 展示 "最小内核 + 用户态驱动" 的设计，Minix 的核心思想 |
| **实施状态** | 待实施 |

#### 4.1.3 L4-like IPC

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 基于共享内存 page 的消息传递 IPC：发送方写消息到共享 page，触发软件中断（`ecall`），接收方读取。实现 `sys_call`（同步 IPC，阻塞等待回复）和 `sys_notify`（异步 IPC，不阻塞）。 |
| **工作量** | ⭐⭐⭐ 约 500 行代码，需设计 IPC 端点和 capability 映射 |
| **与现有资产关联** | 复用 `vm_address_space_map_kernel_range` 做共享页映射，复用 `ecall` 做入口 |
| **教学价值** | L4 微内核的骨架，理解 "fast IPC path" 的核心优化 |
| **实施状态** | 待实施 |

---

### 4.2 安全与隔离

#### 4.2.1 用户态 ASLR

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 在 ELF 加载时随机化用户栈和堆的基地址，增加缓冲区溢出攻击难度。随机化范围由内核在加载时生成，写入 `vm_process_t` 的 `entry_pc` 和 `user_sp`。 |
| **工作量** | ⭐ 约 80 行代码，利用现有 `timer` 的 mtime 做随机种子 |
| **与现有资产关联** | 复用 `user_task_prepare_standard` 的栈初始化路径 |
| **教学价值** | 展示现代 OS 的基本安全机制 |
| **实施状态** | 待实施 |

#### 4.2.2 系统调用沙箱（简化 seccomp）

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 为每个进程维护一个系统调用白名单位图（类似 seccomp-bpf 的简化版），限制进程可调用的系统调用集合。非法调用触发 `SIGKILL` 或直接 panic（教学简化）。 |
| **工作量** | ⭐ 约 120 行代码，在 `trap_dispatch.c` 的 ecall handler 中增加检查 |
| **与现有资产关联** | 复用 `vm_process_t` 增加 `syscall_mask` 字段 |
| **教学价值** | 展示 "最小权限原则"，Chrome 沙箱的核心思想 |
| **实施状态** | 待实施 |

#### 4.2.3 Capability-Based 访问控制

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 文件描述符升级为 capability token，包含权限位（read / write / seek / close / dup）。进程间传递 fd 时传递的是 capability 的引用，而非裸指针。内核维护全局 capability 表，防止伪造。 |
| **工作量** | ⭐⭐⭐ 约 400 行代码，需重新设计文件描述符表 |
| **与现有资产关联** | 复用 `vm_process_t` 的 `user_regions` 数组结构，扩展为 capability 数组 |
| **教学价值** | Capability 安全模型的教学级实现，seL4 的核心设计 |
| **实施状态** | 待实施 |

#### 4.2.4 崩溃隔离 + 可观察 Crash Report ⭐ 本项目独有

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 课程要求"用户程序崩溃不影响内核稳定性"。在此基础上做到**可观察的崩溃隔离**：当用户进程触发 page fault / illegal instruction / 除零等致命异常时，内核不 panic，而是：① 捕获 `sepc`/`scause`/`stval`/GPR 快照生成 crash report 字符串，写入该进程的 `/proc/<pid>/crash_log`；② 通过**内核可观察性协议**（见 4.4）将 crash 事件推送到模拟器的 debug snapshot，前端仪表盘以红色高亮标记崩溃进程；③ 进程进入 zombie 态等待父进程 `waitpid()` 回收。内核和 shell 继续正常运行。 |
| **工作量** | ⭐ 约 150 行代码，在 `trap_dispatch.c` 的异常分发中增加用户态致命异常捕获分支 |
| **与现有资产关联** | **直接复用** `debug_snapshot` 协议做 crash report 推送，复用 `vm_process_t` 增加 `exit_code`/`crash_info` 字段，复用 `/proc`（3.5）暴露 crash_log |
| **教学价值** | 展示 "fail-fast + rich diagnostics" 的设计哲学。答辩时故意运行一个会崩溃的程序，前端实时显示红色 crash 标记但 shell 正常运行，对比传统课程方案的"一崩全死" |
| **实施状态** | 待实施 |

#### 4.2.5 故障注入与恢复演示

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 在 shell 和 debug CLI 中提供受控故障注入入口，用于主动触发用户态非法访问、缺页、非法 syscall、坏 ELF、磁盘 not-ready / bad-magic / bad-block-count、文件系统写入中断等场景。内核应捕获错误、生成可读诊断、保持 shell 和其他进程继续运行，并在前端或 `/proc/<pid>/crash_log` 中留下事件记录。 |
| **工作量** | ⭐ 约 150-250 行代码，按故障类型逐步接入 |
| **与现有资产关联** | 复用 `kernel_alpha` 负向回归思路、`storage` 错误合同、`trap_dispatch.c` 用户态异常分发、`debug_snapshot` 事件面和前端 terminal |
| **教学价值** | 直接证明「用户程序崩溃不影响内核稳定性」「设备错误不会拖垮系统」「文件系统能恢复或明确拒绝不一致状态」。这是答辩中比单纯功能展示更有说服力的健壮性创新 |
| **实施状态** | 待实施 |

---

### 4.3 容器 / 虚拟化方向

#### 4.3.1 轻量容器隔离

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 基于已有的 `vm_address_space_t` 切换，实现"容器"：共享内核，但拥有独立的地址空间、文件系统视图（chroot 简化版）和进程 PID 命名空间。 |
| **工作量** | ⭐ 约 300 行代码 |
| **与现有资产关联** | **直接复用** `vm_address_space_t` 和 `user_task` 的隔离能力 |
| **教学价值** | 展示 "命名空间 + 隔离" 的容器核心思想 |
| **实施状态** | 待实施 |

#### 4.3.2 嵌套虚拟化（教学级）

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 利用 Sv39 的两级页表，让 guest OS 运行在自己的地址空间中，通过 trap-and-emulate 模拟特权指令（`sfence.vma`、`sret`、`wfi` 等）。向"在自己的模拟器里跑另一个 OS"迈进。 |
| **工作量** | ⭐⭐⭐⭐ 约 800 行代码 |
| **与现有资产关联** | 复用 `vm_address_space_t` 的页表切换和 `trap_dispatch.c` 的异常分发 |
| **教学价值** | KVM/VMware 的简化骨架，理解 "虚拟化 = 陷阱 + 模拟" |
| **实施状态** | 待实施（建议作为远期 stretch） |

### 3.5 `/proc` 虚拟文件系统

课程要求实现文件系统（RAMFS 或简化 EXT2）。`/proc` 是一个不占用磁盘的伪文件系统——读取时内核动态生成内容，写入时触发内核操作。它和 `/console` 的前端仪表盘形成**双层可观察性**：前端提供可视化，`/proc` 提供命令行可查询的内核状态接口。

#### 3.5.1 `/proc` 核心接口

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 在文件系统层实现 `/proc` 挂载点，其下的文件和目录不对应任何磁盘 inode，而是在 `read()` 时动态生成内容。至少提供：`/proc/ps`（进程列表：PID/状态/CPU%/内存/命令名）、`/proc/meminfo`（物理内存：total/free/used/pages）、`/proc/cpuinfo`（CPU 信息：ISA/后端/L1D 状态）、`/proc/uptime`（系统运行时间）。`/proc/<pid>/` 子目录按进程展示：`status`、`maps`（地址空间布局）、`fd`（打开文件列表）。 |
| **工作量** | ⭐ 约 250 行代码，在 VFS 层增加 `procfs` 文件系统类型 |
| **与现有资产关联** | 复用 `pmm` 的统计接口（`total_pages/free_pages/used_pages`）、`vm_process_t` 的进程结构、`pipeline_backend` 的 `debug_snapshot`（用于 `cpuinfo`） |
| **教学价值** | 展示 "文件即接口" 的 Unix 哲学。`cat /proc/ps` 和 shell 的 `ps` 命令在底层走同一个系统调用，体现 "一切皆文件" |
| **实施状态** | 待实施 |

#### 3.5.2 `/proc` 写接口

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 部分 `/proc` 文件支持写操作来控制内核行为：`echo 20 > /proc/sched_timeslice` 动态修改时间片长度；`echo 1 > /proc/l1d_enable` 开关 L1D 缓存（如果模拟器暴露了该开关）。写操作触发内核重新配置，无需重启。 |
| **工作量** | ⭐ 约 100 行代码，在 procfs 的 `write()` 路径增加参数解析和配置应用 |
| **与现有资产关联** | 复用 `Machine` 的 L1D 开关（`--l1d`）和调度器的时间片参数 |
| **教学价值** | 展示 "文件系统作为内核配置接口" 的设计模式，Linux sysfs 的简化版 |
| **实施状态** | 待实施 |

---

### 3.6 Shell 管道链与脚本支持

课程要求 shell 支持命令解析、参数传递、管道。在此基础上可以做到一个可演示脚本自动化的完整 shell。

#### 3.6.1 多级管道 + 文件重定向

| 属性 | 内容 |
|---|---|
| **级别** | 深度 |
| **说明** | 实现完整的管道链语法 `cmd1 | cmd2 | cmd3`：内核为管道分配环形缓冲区（利用现有的 `vm_object_t` anon backing），每个管道段 fork 一个子进程，用文件描述符重定向连接 stdin/stdout。同时支持文件重定向：`cmd < input.txt`、`cmd > output.txt`、`cmd >> output.txt`（追加）、`cmd 2> error.log`（stderr 重定向）。 |
| **工作量** | ⭐⭐⭐ 约 400 行代码，需在 shell 解析器中增加管道分割和重定向符号识别 |
| **与现有资产关联** | 复用 `vm_object_t` 的 anon page 做管道缓冲区，复用文件描述符表做重定向 |
| **教学价值** | 展示 Unix pipeline 的完整实现：fork + dup2 + close + exec。这是经典 OS 面试题 |
| **实施状态** | 待实施 |

#### 3.6.2 Shell 脚本执行

| 属性 | 内容 |
|---|---|
| **级别** | 轻量 |
| **说明** | 在管道和重定向基础上，shell 增加脚本模式：`sh /path/to/script.sh` 读取脚本文件，逐行解析执行。支持 `#!/bin/sh` shebang 识别、注释行（`#`）、环境变量设置（`VAR=value`）。不需要完整的 bash 兼容——只需要能让下面的脚本跑起来：
```sh
#!/bin/sh
echo "=== myOS System Report ==="
cat /proc/cpuinfo
cat /proc/meminfo
cat /proc/ps
```
这就可以在答辩中用一条命令完成所有功能点展示。 |
| **工作量** | ⭐ 约 150 行代码，在 shell 主循环增加文件读取模式和简单变量替换 |
| **与现有资产关联** | 复用 3.6.1 的管道/重定向和 3.5 的 `/proc` 文件系统 |
| **教学价值** | 答辩时运行 `sh /demo.sh` 一键展示所有功能点，脚本本身就是"活的设计文档" |
| **实施状态** | 待实施 |

---

## 五、推荐组合方案（按投入产出排序）

> 以下组合方案供各 agent 评审和选择。请根据团队规模、时间预算和兴趣方向做取舍。

### 方案 A：最小投入 + 1 个王炸（推荐 1 人团队）

**必做（100% 满足课程要求）：**
1. 进程管理：FCFS + RR + **MLFQ**（在 RR 基础上自然扩展）
2. 内存管理：Demand Paging + **Clock 页面置换**
3. 文件系统：RAMFS + **COW Snapshot**（利用已有 anon 对象机制）

**选做（选 1 个深度方向）：**
- **AI Shell + NPU 中断驱动调度**（利用已有 NPU，工作量适中，答辩效果爆炸）

**预期答辩演示：**
```
$ ai-run tiny_model        # 用户态 shell 命令
[AI] submitting graph...   # 进程进入阻塞态
[NPU] DMA load → compute → DMA store   # 前端实时可视化
[AI] result: [42, 17, ...] # shell 输出推理结果
$ ps                       # 可以看到 ai-run 进程的完整状态转换
```

### 方案 B：算法深度 + 性能（推荐 2 人团队）

**必做：**
1. 调度：FCFS + RR + **CFS 简化版**（排序链表维护 vruntime）
2. 内存：**伙伴系统** + **WSClock**
3. 文件系统：RAMFS + **B 树目录索引**

**选做：**
- **Pipeline 感知调度 + `sys_perf`**（调度器根据 CPU 流水线状态动态决策）
- **JIT 沙箱隔离**（DBT translation 阶段插入安全检查）

### 方案 C：架构探索 + 安全（推荐 2-3 人团队）

**必做：**
1. 调度：FCFS + RR + **优先级继承**
2. 内存：Demand Paging + **SLAB 分配器**
3. 文件系统：**微内核 FS 服务化**（FS 运行在用户态进程）

**选做：**
- **L4-like IPC**（共享内存 + 消息传递）
- **Capability-Based 访问控制**（文件描述符升级为 capability token）
- **系统调用沙箱**（简化 seccomp）

### 方案 D：全栈王炸（推荐 3 人团队，时间充裕）

**核心模块全部覆盖课程要求 + 以下创新：**
1. **AI Accelerator 全链路**：`sys_ai_submit` → NPU 中断驱动调度 → AI Shell → AI 任务调度器 → 前端 NPU 推理可视化
2. **Pipeline + JIT + 调度协同**：`sys_perf` → Pipeline 感知调度 → 自适应 JIT 调度
3. **微内核 + 安全**：FS 服务化 + L4-like IPC + Capability-Based 访问控制 + ASLR
4. **前端可视化**：进程状态机动画 + 内存热力图 + 调度过程交互式回放

### 方案 E：课程满分基线 + 全栈可观测主创新（Codex 推荐）

**定位：** 先百分百完成课程文档要求，再选择一条最能体现本项目差异化的创新主线。该方案不把 xv6 / Linux 能力包装成自研 OS 成果，而是把自研 guest OS、模拟器、debug CLI 和浏览器 Lab 串成「可运行、可解释、可验证」的教学 OS 平台。

**硬基线（必须完整满足课程要求）：**
1. 启动：M 态到 S 态切换、内核加载、`stvec` / trap frame、C runtime、启动日志。
2. 中断与异常：timer、UART / 键盘输入、`ecall` syscall、用户态 page fault、用户崩溃隔离。
3. 内存：PMM、`kmalloc/kfree`、Sv39、用户代码 / 数据 / 堆 / 栈布局、Demand Paging、泄漏检查。
4. 进程：PCB、`fork/exec/exit/wait/waitpid`、FCFS + RR、状态转换、信号量 / 互斥锁。
5. 文件系统：RAMFS 或简化磁盘 FS、目录 / 文件 CRUD、fd 表、路径解析、seek、重定向、`mkfs`。
6. 用户程序：ELF 加载、简化 libc、至少 5 个用户程序、shell 支持参数、管道和外部程序执行。

**主创新组合：**
1. **全栈可观测教学 OS**：优先实现 3.4.6、3.5 和 3.6。用 `/proc` 与前端 Lab 同时解释同一份内核状态，让 `ps`、`cat /proc/meminfo`、前端进程面板和 debug snapshot 互相印证。
2. **可插拔调度器 + 可量化指标**：在 FCFS / RR 基础上补 MLFQ 或 CFS-lite，并记录 average wait time、turnaround time、context switch count、preempt count。答辩时用同一组 workload 对比不同算法。
3. **Demand Paging + COW Fork**：让 `fork()` 的页面复制从 eager copy 变成 fault-driven COW，并在 `/proc/meminfo` 或前端展示 saved pages、COW faults、page refcount。
4. **故障注入 + Crash Report**：补 4.2.4 和 4.2.5。用一个故意崩溃的用户程序证明内核不崩、父进程能 `waitpid()`、shell 继续工作，前端和 `/proc/<pid>/crash_log` 能看到原因。
5. **文件系统恢复能力（可选 stretch）**：如果 RAMFS / 简化磁盘 FS 已稳定，再做 mini journal 或写入事务。答辩时模拟写一半失败，再通过 replay / fsck 恢复。

**建议演示脚本：**

```sh
echo "=== boot and system profile ==="
cat /proc/cpuinfo
cat /proc/meminfo
ps

echo "=== scheduler comparison ==="
sched rr 10
run workload_io &
run workload_cpu &
cat /proc/schedstat
sched mlfq
run workload_mix
cat /proc/schedstat

echo "=== demand paging and COW ==="
cowdemo 64K
cat /proc/meminfo

echo "=== filesystem and shell ==="
mkdir /demo
echo hello > /demo/a.txt
cat /demo/a.txt | wc

echo "=== crash isolation ==="
crashdemo
cat /proc/last_crash
ps
```

**取舍建议：** 如果时间紧，优先做「全栈可观测 + MLFQ + COW + crash report」。AI / JIT / Pipeline 感知调度更有辨识度，但应作为加分项，不能影响课程六大模块的完整交付。

---

## 六、Agent 署名与贡献区

> 每个 agent 在以下模板中追加自己的署名和创新提案。已实施的创新请在对应条目下更新「实施状态」。

### Agent: Kimi Code CLI（初始版本）

- **贡献内容**：本文档全部初始内容（算法 / 功能 / 架构三个维度的 25+ 创新方向）
- **核心主张**：本项目的创新空间不在于"把课程要求做一遍"，而在于"把课程六大模块与项目独有资产（NPU、JIT/DBT、Pipeline OoO、前端 Lab）做有机连接"
- **推荐优先级**：
  1. 最高：AI 系统调用 + NPU 中断驱动调度（利用已有 NPU，工作量适中，效果爆炸）
  2. 次高：MLFQ + Clock 置换（算法层面，代码量小，教学价值高）
  3. 可选：微内核 FS 服务化 / Pipeline 感知调度 / Capability 访问控制

---

### Agent: Reasonix Code（第一轮补充）

- **贡献内容**：新增 7 个创新条目，侧重"与模拟器独有资产的深度连接"和"可演示性"：
  1. **2.2.5 COW Fork**：进程级写时复制 fork，复用现有 `vm_object_t` 引用计数和 `vm_fault.c` 的 store fault 路径
  2. **2.3.4 自适应预读块缓存**：LRU + 顺序检测 + 窗口自适应扩展/收缩
  3. **3.5 `/proc` 虚拟文件系统**：`/proc/ps`、`/proc/meminfo`、`/proc/cpuinfo`、`/proc/<pid>/` 子目录，与前端仪表盘形成双层可观察性
  4. **3.6 Shell 管道链与脚本支持**：多级管道 + 文件重定向 + `sh script.sh` 脚本执行
  5. **4.2.4 崩溃隔离 + 可观察 Crash Report**：用户进程崩溃时生成 crash report，通过 KOP 推送前端红色标记，内核继续运行
  6. **4.4 KOP 内核可观察性协议**：guest 内核 ↔ 模拟器 ↔ 浏览器的三层结构化观测协议，是 3.4 全部前端可视化的架构基础
  7. **4.5 模拟器-内核协同快速启动**：模拟器预置页表+ramdisk，guest 跳过 bring-up，启动时间从 500ms → 50ms
- **核心主张**：创新点之间应该形成"协议 → 面板 → 命令"的闭环，而不是孤立罗列。KOP（4.4）是架构基础，驱动前端面板（3.4）；`/proc`（3.5）是命令行侧的双生接口；shell 脚本（3.6）是答辩时一键串联全部功能点的演示引擎。三者叠加形成「观测协议 → 可视化 → 命令行 → 脚本自动化」的完整叙事线。
- **实施状态更新**：全部条目为待实施状态

---

### Agent: Codex（第二轮整合补充）

- **贡献内容**：在已有条目基础上补充并综合为一条课程答辩主线：
  1. 新增 **3.4.6 全栈可观测教学 OS 面板**，把进程、调度、syscall、trap、页表、PMM、文件系统和 block cache 纳入统一可观测面。
  2. 新增 **4.2.5 故障注入与恢复演示**，把课程要求中的稳定性、异常处理和设备错误恢复做成可主动触发、可解释、可回归的演示能力。
  3. 新增 **方案 E：课程满分基线 + 全栈可观测主创新**，明确所有课程要求必须先完整满足，创新主线优先选择「可观测 + 可量化 + 可恢复」而不是堆叠无关功能。
- **核心主张**：本项目最适合的主创新不是单点算法，而是「自研 OS + 自研模拟器 + debug CLI + 浏览器 Lab」形成的可观测教学平台。普通课程项目只能证明 OS 能跑；本项目可以证明 OS 为什么这样跑、哪里发生了 trap、哪个进程被调度、哪一页触发 COW、哪个文件描述符被重定向、崩溃为什么没有拖垮内核。
- **推荐优先级**：
  1. 最高：全栈可观测教学 OS（3.4.6 + 3.5 + 3.6）
  2. 次高：可插拔调度器指标化（FCFS / RR 基线 + MLFQ 或 CFS-lite）
  3. 次高：Demand Paging + COW Fork + `/proc/meminfo` 证据面
  4. 加分：故障注入 + Crash Report + 前端红色事件标记
  5. Stretch：AI Shell、Pipeline 感知调度、JIT profile、mini journal FS
- **实施状态更新**：全部条目为待实施状态

---

### Agent: [待补充]

- **贡献内容**：
- **实施状态更新**：

---

## 附录：课程要求速查表

> 方便各 agent 快速对照课程文档的功能点，避免遗漏。

| 模块 | 功能点 | 课程技术指标 |
|---|---|---|
| 系统启动 | M→S 切换、加载内核、中断向量表、栈初始化、启动日志 | 稳定启动，无异常重启 |
| 中断与异常 | 时钟中断、键盘中断、ecall、缺页异常 | 时钟 100Hz，键盘中断→ASCII，系统调用支持打印/进程创建/退出/文件读写 |
| 内存管理 | 物理内存探测、页框分配器、虚拟内存、kmalloc/kfree、用户态内存布局 | ≥4MB 物理内存，4KB 页，Demand Paging，无泄漏 |
| 进程管理 | PCB、fork/exec、调度器（FCFS+RR）、状态转换、信号量/互斥锁、wait/waitpid | 多并发进程，时间片可配置（默认 10ms），切换开销 <1ms，父子关系树 |
| 文件系统 | 简化 FS、文件/目录 CRUD、文件描述符表、路径解析、IO 重定向 | ≥128 文件，单文件 ≤64KB，≥3 层目录，支持 seek，提供 mkfs |
| 用户程序加载 | ELF 解析加载、用户栈初始化、libc 简化版、shell（ls/cat/echo/ps/kill/exec） | ≥5 个不同用户程序，shell 支持参数传递和管道，用户崩溃不影响内核 |

> **管理评价**：至少 3 个模块，9 个功能点。评分包括：汇报答辩、设计方案文档、演示文稿、视频、功能点、创新点。
